# Flitube

A CLI tool to stream YouTube music from your terminal.

## Setup

1. Install FFmpeg
2. Install dependencies:
   ```bash
   npm i
   ```
3. Run the application:
   ```bash
   node index.js
   ```
